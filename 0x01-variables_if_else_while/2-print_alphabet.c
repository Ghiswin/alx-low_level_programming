#include <stdlib.h>
#include <time.h>
#include <stdio.h>

/**
*main -> a random number to the variable n each time it is executed and print
*based a condition
*return : always 0
*/
int main(void)
{
int ch;
for (ch = 97; ch <= 122; ch++)
{
putchar(ch);     	
}
putchar(10); /* this is an new line up*/

return (0);
}


